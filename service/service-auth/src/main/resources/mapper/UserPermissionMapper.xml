<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hjf.job.auth.mapper.UserPermissionMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.hjf.job.auth.entity.UserPermission">
                    <result column="user_id" property="userId" />
                    <result column="granter_id" property="granterId" />
                    <result column="permission_id" property="permissionId" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="columns">
                user_id, granter_id, permission_id
        </sql>

</mapper>
