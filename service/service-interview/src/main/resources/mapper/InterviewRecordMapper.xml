<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hjf.job.interview.mapper.InterviewRecordMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.hjf.job.interview.entity.InterviewRecord">
                    <result column="position_id" property="positionId" />
                    <result column="candidate_id" property="candidateId" />
                    <result column="creator_id" property="creatorId" />
                    <result column="note" property="note" />
                    <result column="current_stage" property="currentStage" />
                    <result column="status" property="status" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="columns">
                position_id, candidate_id, creator_id, note, current_stage, status
        </sql>

</mapper>
