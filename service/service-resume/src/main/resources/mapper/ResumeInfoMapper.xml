<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hjf.job.resume.mapper.ResumeInfoMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.hjf.job.model.entity.resume.ResumeInfo">
                    <result column="candidate_id" property="candidateId" />
                    <result column="resume_name" property="resumeName" />
                    <result column="job_status" property="jobStatus" />
                    <result column="personal_advantages" property="personalAdvantages" />
                    <result column="professional_skills" property="professionalSkills" />
                    <result column="is_default_display" property="isDefaultDisplay" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="columns">
                candidate_id, resume_name, job_status, personal_advantages, professional_skills, is_default_display
        </sql>

</mapper>
