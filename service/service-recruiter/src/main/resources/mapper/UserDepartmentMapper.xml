<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hjf.job.recruiter.mapper.UserDepartmentMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="cn.hjf.job.model.entity.recruiter.UserDepartment">
                    <result column="department_id" property="departmentId" />
                    <result column="user_id" property="userId" />
                    <result column="is_manager" property="isManager" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="columns">
                department_id, user_id, is_manager
        </sql>

</mapper>
